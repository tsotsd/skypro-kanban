import Login from "../../components/Login/Login";

export const LoginPage = ({login}) => {
    return (
           <>
            <Login login={login}/>
            </>
    )
}

export default LoginPage;